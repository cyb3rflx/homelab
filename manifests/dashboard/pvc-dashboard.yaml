apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-dashboard
  namespace: dashboard
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 2Gi
  storageClassName: ""
  volumeName: pv-dashboard
